function calcRefSpots(Pupil_Plane, Pitch, PixRatio)
    %-- STEP 1: Given the entire image, divide the image into ideally
    %-- equally spaced windows, such that they perfectly fit onto the image
    %-- without overlapping with each other.
    
    %-- ARGUMENTS: Pupil_Plane = The image as a matrix with each element
    %   being it's pixel intensity
        
end
  